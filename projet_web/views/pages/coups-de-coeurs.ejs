<!DOCTYPE html>
<html lang="fr">
<head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/coupCoeur.css">
    <title>Vos coups de cœur</title>
</head>
<body>
    <%- include('../partials/header') %>

    <main class="coups-coeur-container">
        <h1 class="page-title">Vos coups de cœur</h1>
    
        <% if (tomes.length === 0) { %>
            <p class="aucun-coup-de-coeur">Vous n'avez aucun coup de cœur pour le moment.</p>
        <% } else { %>
            <% tomes.forEach(tome => { %>
                <div class="ligne-manga">
                    <!-- Image -->
                    <div class="image-container">
                        <img src="<%= tome.image %>" alt="<%= tome.titre_serie %> Tome <%= tome.numero_volume %>">
                    </div>
    
                    <!-- Infos -->
                    <div class="infos-container">
                        <p class="titre"><%= tome.titre_serie %></p>
                        <p class="tome">Tome <%= tome.numero_volume %></p>
                        <p class="auteur"><%= tome.auteur %></p>
                        <p class="editeur"><%= tome.editeur %></p>
                    </div>
    
                    <!-- Prix et boutons -->
                    <div class="actions-container">
                        <p class="prix"><%= tome.prix ? tome.prix.toFixed(2) : 'Prix inconnu' %> $</p>
                        <form action="/panier/<%= tome.isbn %>" method="POST">
                            <button type="submit" class="btn-panier">Ajouter au panier</button>
                        </form>
                        <form action="/coupCoeur/<%= tome.isbn %>/supprimer" method="POST">
                            <button type="submit" class="btn-supprimer">Supprimer</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        <% } %>
    </main>
    
    

    <%- include('../partials/footer') %>
</body>
</html>
