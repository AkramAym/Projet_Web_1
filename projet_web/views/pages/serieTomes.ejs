<!DOCTYPE html>
<html lang="fr">
<head>
    <%- include('../partials/head') %> <!-- Inclusion du header -->
    <title><%= serie.titre_serie %> - Tomes</title>
</head>
<body>
    <div class="serie-container">
        <!-- Image Longue en bannière -->
        <div class="serie-banner <%= serie.titre_serie.toLowerCase().replace(/\s/g, '-') %>">
            <img src="<%= serie.imageLong_serie %>" alt="Bannière de <%= serie.titre_serie %>">
        </div>
        
        
        <!-- Liste des tomes -->
        <%- include('../partials/tomes') %>
        <!--
        <div class="tomes-list">
            <% tomes.forEach(tome => { %>
                <div class="tome-card">
                    <a href="/tome/<%= tome.isbn %>">
                        <img src="<%= tome.image %>" alt="Couverture du tome <%= tome.numero_volume %>">
                        <p><%= tome.titre_serie %> Tome <%= tome.numero_volume %></p>
                        <p><%= tome.prix %> $</p>
                    </a>
                    <button class="favorite-btn"><i class="fa-regular fa-heart"></i></button>
                </div>
            <% }) %>
        </div>
        -->
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
<footer>
    <%- include('../partials/footer') %>
</footer>
</html>